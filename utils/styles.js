import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  textField: {
    width: '90%',
    marginLeft: 'auto',
    marginRight: 'auto',
    color: 'white',
    paddingBottom: 0,
    marginTop: 0,
    fontWeight: 500
},
  error: {
    color: '#f04040',
  },
});
export default useStyles;
